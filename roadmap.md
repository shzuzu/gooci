# 🚀 Goci CI/CD — Roadmap проекта

Лёгкая самодостаточная CI/CD система, вдохновлённая GitHub Actions, написанная на Go.

---

## 📊 Статус этапов

| Этап | Название                       | Статус          |
| ---- | ------------------------------ | --------------- |
| 1    | CLI Task Runner (Ядро)         | 🟩 Завершён     |
| 2    | Dockerized Runner              | 🟨 В разработке |
| 3    | REST API + Build Server        | 🟦 Не начат     |
| 4    | Очередь задач и Worker System  | 🟦 Не начат     |
| 5    | Web UI + WebSocket логирование | 🟦 Не начат     |
| 6    | Git Webhooks интеграция        | 🟦 Не начат     |
| 7    | Параллельное выполнение шагов  | 🟦 Не начат     |
| 8    | Регистрация раннеров           | 🟦 Не начат     |
| 9    | Multi-Tenant система           | 🟦 Не начат     |

**Статусы:** 🟦 Не начат | 🟨 В разработке | 🟩 Завершён

---

## 📍 Видение проекта

Создать модульную CI/CD платформу производственного уровня, способную запускать пайплайны локально, в Docker-контейнерах и на распределённых воркерах — с акцентом на простоту, безопасность и обратную связь в реальном времени.

---

## 🗺️ Этапы разработки

### Этап 1 — CLI Task Runner (Ядро системы)

**Цель:** Запуск пайплайнов локально из YAML-файлов.

```bash
goci run pipeline.yaml
```

**Возможности:**

- Парсинг YAML-определений пайплайнов
- Последовательное выполнение шагов
- Обработка ошибок и кодов выхода
- Логирование в консоль

**Пример `pipeline.yaml`:**

```yaml
steps:
  - name: Install
    run: go mod tidy

  - name: Test
    run: go test ./...

  - name: Build
    run: go build -o app
```

**Технологии:**

- Go
- `os/exec`
- `gopkg.in/yaml.v3`

**✅ MVP:** CLI исполняет YAML-пайплайны с корректным отображением статуса.

---

### Этап 2 — Dockerized Runner

**Цель:** Запуск каждого пайплайна внутри изолированного Docker-контейнера.

**Возможности:**

- Интеграция с Docker API из Go
- Монтирование томов для исходного кода
- Потоковая передача логов контейнера
- Управление жизненным циклом контейнера

**Пример:**

```yaml
image: golang:1.22
steps:
  - run: go test ./...
```

**Технологии:**

- Docker Engine API
- `github.com/docker/docker/client`
- Управление контекстом
- Потоковое логирование

**✅ MVP:** CLI запускает контейнеры, исполняет пайплайны, передаёт логи.

---

### Этап 3 — REST API + Build Server

**Цель:** Превратить CLI в бэкенд-сервис.

**API Endpoints:**

| Method | Endpoint          | Описание              |
| ------ | ----------------- | --------------------- |
| `POST` | `/build`          | Запустить билд        |
| `GET`  | `/build/:id`      | Получить статус билда |
| `GET`  | `/build/:id/logs` | Получить логи билда   |

**Архитектура:**

1. API принимает YAML
2. Сохраняет в базу данных
3. Запускает worker-горутину
4. Обновляет статус в БД

**Технологии:**

- `net/http` или `chi`
- PostgreSQL
- Горутины и примитивы синхронизации
- Отмена через context

**✅ MVP:** Запуск билдов через curl, проверка статуса, получение логов.

---

### Этап 4 — Очередь задач и Worker System

**Цель:** Разделить API и исполнение с помощью распределённой архитектуры воркеров.

**Архитектура:**

```
API → Queue → Worker(s)
```

**Возможности:**

- Распределение задач через очередь
- Множество параллельных воркеров
- Политики повторных попыток
- Восстановление после сбоев

**Технологии:**

- Redis / NATS / PostgreSQL (как очередь)
- Паттерны идемпотентности
- Проектирование распределённых систем

**✅ MVP:** Несколько воркеров обрабатывают задачи из общей очереди.

---

### Этап 5 — Web UI + WebSocket логирование

**Цель:** Мониторинг билдов в реальном времени в браузере.

**Возможности:**

- Дашборд с историей билдов
- Страницы отдельных билдов
- Потоковая передача логов в реальном времени
- Индикаторы статуса билдов

**Технологии:**

- WebSocket / SSE
- React / Svelte / HTMX
- Управление буфером
- Логика переподключения

**✅ MVP:** Логи отображаются в браузере в реальном времени во время выполнения.

---

### Этап 6 — Git Webhooks интеграция

**Цель:** Автоматические билды по событиям Git.

**Поддерживаемые платформы:**

- GitHub
- GitLab

**Возможности:**

- Валидация подписи webhook (HMAC)
- Парсинг payload
- Автоматическое клонирование репозитория
- Запуск пайплайна при push/PR

**✅ MVP:** Push в GitHub автоматически запускает билд.

---

### Этап 7 — Параллельное выполнение шагов

**Цель:** Поддержка параллельных шагов пайплайна с выполнением на основе DAG.

**Пример:**

```yaml
steps:
  - name: Test Backend
    run: go test ./backend
    parallel: true

  - name: Test Frontend
    run: npm test
    parallel: true
```

**Возможности:**

- Выполнение на основе ориентированного ациклического графа (DAG)
- Топологическая сортировка
- Синхронизация шагов

**✅ MVP:** Параллельные шаги выполняются одновременно с правильной обработкой зависимостей.

---

### Этап 8 — Регистрация раннеров

**Цель:** Распределённая система раннеров (аналогично self-hosted раннерам GitHub Actions).

**Возможности:**

- Регистрация раннера на сервере
- Аутентификация на основе токенов
- Механизм heartbeat
- Динамическое масштабирование воркеров

**✅ MVP:** Раннеры могут регистрироваться, получать задачи и отправлять статус.

---

### Этап 9 — Multi-Tenant система

**Цель:** Готовая к продакшену многопользовательская платформа.

**Возможности:**

- Учётные записи пользователей и аутентификация
- Управление проектами
- Ролевая модель доступа (RBAC)
- Изоляция пайплайнов
- JWT токены

**✅ MVP:** Безопасная многопользовательская система с правильными разрешениями.

---

## 📋 Приоритет реализации

```
1. Этап 1 → Надёжная базовая архитектура
2. Этап 2 → Глубокое понимание Docker API
3. Этапы 3–4 → Архитектура воркеров и конкурентность
4. Этап 5 → UI слой
5. Этапы 6+ → Git интеграция и продвинутые возможности
```

---

## ⚠️ Известные вызовы

| Вызов                                       | Решение                                                  |
| ------------------------------------------- | -------------------------------------------------------- |
| Преждевременная разработка UI               | Фокус на ядре системы в первую очередь                   |
| Отсутствие политик retry                    | Реализовать на раннем этапе в системе воркеров           |
| Неполная обработка ошибок                   | Проектировать восстановление после сбоев с самого начала |
| Пробелы в безопасности (побеги контейнеров) | Правильное песочирование и изоляция                      |

---

## 🛠️ Сводка технологического стека

| Компонент                    | Технология            |
| ---------------------------- | --------------------- |
| Язык                         | Go                    |
| Среда выполнения контейнеров | Docker                |
| База данных                  | PostgreSQL            |
| Очередь                      | Redis / NATS          |
| Фронтенд                     | React / Svelte / HTMX |
| Real-time                    | WebSocket / SSE       |

---

## 📈 Критерии успеха

- ✅ Локальное выполнение пайплайнов работает надёжно
- ✅ Docker изоляция предотвращает доступ к хост-системе
- ✅ API обрабатывает параллельные билды без состояний гонки
- ✅ Воркеры корректно восстанавливаются после сбоев
- ✅ Логи реального времени передаются без значительной задержки
- ✅ Git webhooks автоматически запускают билды

---

## 🎯 Долгосрочное видение

> Лёгкая самодостаточная CI/CD альтернатива, которая сочетает простоту локального выполнения с мощью распределённых систем — подходит для команд, которые хотят полностью контролировать свою инфраструктуру сборок.

---

_Последнее обновление: Февраль 2026_
